# Python tooling for the bulk RFC import pipeline.
#   pip install -r tools/requirements.txt
pdfplumber>=0.11.0    # PDF text + table extraction (parse_pdf.py)
openpyxl>=3.1.0       # Excel tracker reader (read_tracker.py)
